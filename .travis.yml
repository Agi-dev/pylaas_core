language: python
python:
  - "3.6"
  - "nightly"

# command to install dependencies
install:
  - "pip install -e . --upgrade"
  - "pip install pipenv"
  - "pipenv install --dev"

# command to run tests
script:
  - pipenv run python -m pytest tests # or py.test for Python versions 3.5 and below